"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok){setError((await r.json()).error??"Unable to sign in");return}router.push((await r.json()).redirect);router.refresh()}return <main className="login"><section className="login-card"><div className="seal">CL</div><p className="eyebrow">CENTRAL LIBRARY</p><h1>HOURS MANAGEMENT<br/>SYSTEM</h1><p className="muted">Secure internal access</p><form onSubmit={submit}><label>Username / Email<input name="email" type="email" required autoComplete="username"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="error">{error}</p>}<button>Login</button></form></section></main>}
